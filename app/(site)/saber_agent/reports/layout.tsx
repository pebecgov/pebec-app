import { Metadata } from "next";
export const metadata: Metadata = {
    title: "Saber Agent - Submit Report ",
};

export default function SubmitReportLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}