import { Metadata } from "next";
export const metadata: Metadata = {
    title: "ReportGov Agent - Recieved Letters",
};

export default function RecievedLettersLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}