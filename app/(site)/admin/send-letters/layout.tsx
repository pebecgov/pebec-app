import { Metadata } from "next";
export const metadata: Metadata = {
    title: "ADMIN - Send Letters",
};
export default function SendLettersLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
    