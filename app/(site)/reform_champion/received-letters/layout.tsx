import { Metadata } from "next";
export const metadata: Metadata = {
    title: "Reform Champion - Recieved letters",
};

export default function recieveLetterLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}