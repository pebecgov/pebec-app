import { Metadata } from "next";
export const metadata: Metadata = {
    title: "Reform Champion - Send letter",
};

export default function sendLetterLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}