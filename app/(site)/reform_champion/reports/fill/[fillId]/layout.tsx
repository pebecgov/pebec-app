import { Metadata } from "next";
export const metadata: Metadata = {
    title: "Reform Champion - Fill Report",
};

export default function FillReportLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}